<div class="container">
  <div class="row">
    <div class="col d-flex justify-content-between">
      <h1>
      My Posts
    </h1>
      <button id="new-post-btn" type="button" class="btn btn-primary">New Post</button>
    </div>
  </div>
  <div class="row">
    <div class="col">
      {{#each user.posts as |post|}}
      <div id="{{post.id}}" class="large-card my-4 p-5 mh-auto bg-primary text-white rounded">
        <div class="row">
          <div class="col d-flex justify-content-between">
            <h4 class="align-self-center center">{{post.title}}</h4>
            <div class="btn-group-vertical">
              <button type="button" class="btn btn-warning update-btn">Update</button>
              <button type="button" class="btn btn-danger delete-btn">Delete</button>
            </div>
          </div>
        </div>
      </div>
      {{/each}}
    </div>
  </div>
</div>

<script src="/js/dashboard.js"></script>